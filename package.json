{
  "name": "sawtak",
  "version": "1.0.0",
  "description": "Sawtak - Secure, Transparent Blockchain based reporting platform",
  "scripts": {
    "dev": "concurrently \"bun run --cwd backend dev\" \"bun run --cwd Front-end dev\"",
    "test": "cd backend && bun test --path-ignore-patterns='**/integration/**' && cd ../privacy-proxy && bun test",
    "test:integration": "cd backend && bun test tests/integration"
  },
  "keywords": [
    "Sawtak",
    "reporting",
    "blockchain",
    "transparent",
    "secure",
    "complaint",
    "management",
    "system"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}