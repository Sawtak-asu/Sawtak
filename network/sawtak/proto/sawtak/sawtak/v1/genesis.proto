syntax = "proto3";

package sawtak.sawtak.v1;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "sawtak/sawtak/v1/complaint.proto";
import "sawtak/sawtak/v1/params.proto";

option go_package = "github.com/sayedibrahimQ/sawtak/x/sawtak/types";

// GenesisState defines the sawtak module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
  repeated Complaint complaint_list = 2 [(gogoproto.nullable) = false];
  uint64 complaint_count = 3;
}
